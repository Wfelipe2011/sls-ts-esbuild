{
  "name": "template-base",
  "version": "1.0.0",
  "description": "Serverless template example using Typescript + Esbuild",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "prebuild": "rimraf dist",
    "predeploy": "npm run build",
    "deploy": "npx sls deploy",
    "remove": "npx sls remove"
  },
  "dependencies": {

  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.17",
    "@types/node": "^10.12.18",
    "@types/serverless": "^1.72.5",
    "esbuild": "^0.17.16",
    "rimraf": "^5.0.0",
    "serverless": "^3.30.1",
    "serverless-esbuild": "^1.43.0",
    "ts-node": "^8.10.2",
    "typescript": "^3.2.4"
  }
}
